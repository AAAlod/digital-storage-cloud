# Digital Storage Cloud 玩家使用指南

## 模组有什么用

Digital Storage Cloud 是 Tom's Simple Storage 的大宗物品存储后端。它把可堆叠物品存进属于玩家的数字存储卷，再由“数字存储访问器”把存储卷接入 Tom 网络。

它不会替代 Tom 的终端、搜索、合成终端或线缆网络。推荐分工是：

- 数字存储卷保存圆石、矿物、农作物等大量可堆叠物品；
- 普通箱子保存工具、武器、盔甲和其他不可堆叠物品；
- Tom 终端继续负责统一查看、搜索、存取和合成。

## 安装要求

- Minecraft 1.20.1
- Fabric Loader 0.15.11 或更高版本
- Fabric API
- Java 17
- Tom's Simple Storage 1.7.1（精确版本硬依赖）

将本模组 JAR、Fabric API 和 Tom's Simple Storage JAR 放入游戏或服务器的 `mods` 文件夹。多人游戏时，服务器和需要打开管理界面的客户端都应安装本模组及 Fabric API。

## 合成数字存储访问器

在工作台按下面的形状合成：

```text
金锭        红石          金锭
红石        信标          红石
金锭        库存线缆连接器  金锭
```

总成本为 4 个金锭、3 个红石、1 个信标和 1 个 Tom 库存线缆连接器，
每次合成得到 4 个“数字存储访问器”。

## 合成高级库存漏斗

```text
钻石        红石          钻石
红石        Tom 基础库存漏斗  红石
钻石        红石          钻石
```

总成本为 4 个钻石、4 个红石和 1 个 Tom 基础库存漏斗。普通 Tom 库存
漏斗默认每批搬运 16 件，高级库存漏斗默认每批搬运 64 件。

## 最快上手

1. 放置数字存储访问器并右键打开管理界面。
2. 在“存储卷名称”中输入名称，例如“建筑材料”，点击“创建”。
3. 在存储卷列表中找到刚创建的卷，点击对应的“绑定”按钮。
4. 将 Tom 的库存连接器直接贴在访问器旁边，或用 Tom 的库存线缆连接器把访问器接入现有网络。
5. 打开 Tom 存储终端。现在可以像使用联网箱子一样存取数字存储卷里的物品。

首次成功绑定访问器的玩家会成为该访问器的控制者。只有控制者可以更改或清除它的绑定，而且玩家只能把访问器绑定到自己的存储卷。

## 容量和升级效果

容量按“物品种类”计算，不按箱子槽位或总件数计算。同一种物品即使数量很大，也只占用 1 个种类名额；带不同 NBT 的物品会被视为不同种类。

| 等级 | 可存物品种类 | 升到该等级的默认费用 |
| --- | ---: | ---: |
| 基础 | 64 | 创建存储卷后自动获得 |
| 高级 | 128 | 16 钻石 |
| 精英 | 256 | 32 钻石 |
| 终极 | 512 | 64 钻石 |
| 最高 | 1024 | 128 钻石 |

每个精确物品变体有意设置 `2,147,483,647` 件的安全上限；带不同 NBT 的物品分别计算。已有存档即使超过该上限也仍可取出，只是不再接受新增。升级不会清空已有物品；在绑定界面准备好材料后点击“升级”即可。整合包可通过数据包修改等级容量和费用，因此服务器实际设置可能与默认值不同。

## 多访问器和跨地点使用

- 同一个存储卷可以同时绑定任意数量的访问器。
- 不同地点的访问器只要绑定同一个卷，看到的就是同一批物品。
- 默认每名玩家最多创建 3 个独立存储卷；服务器可修改，设为 0 时不限制数量。
- 默认挖掉访问器后，掉落物不会保留控制者和绑定信息。重新放置后需要再次绑定，但存储卷和其中物品不会被删除。
- 删除存储卷前，必须先取空其中物品，并清除仍在加载中的访问器绑定。

## Tom 网络诊断和自动迁移

打开已绑定访问器时会自动分析一次 Tom 网络；拓扑改变后也可以点击“重新分析”手动刷新。界面会显示“网络性能”：

- `0–100 / A–E` 是相对健康度，用来观察网络中实体库存槽位和扫描负担，不是绝对 TPS 或 MSPT 测量；
- 网络健康时只显示总体状态；重复入口、异常漏斗和可迁移物品只在需要处理时出现，底层计数仍可通过诊断命令查看；
- “推荐优化”会优先挑选适合放入当前数字卷、又能释放较多实体槽位的物品种类；
- 点击“执行推荐迁移”，会把推荐物品从同一 Tom 网络的普通库存逐步搬入当前数字卷；
- 迁移使用事务保护：只有来源成功取出且目标完整接收时才会提交，目标容量变化时不会凭空吞物；
- 迁移期间可点击“取消迁移”。访问器、连接器、来源库存、网络结构或目标失效时，任务会安全停止，不会自动续跑。

如果同一个存储卷以多个访问器重复接入同一 Tom 网络，界面会显示“重复存储卷入口”，并阻止自动迁移。保留一个入口即可；日常存取仍由 Tom 网络完成。

## 物品限制

- 默认拒绝最大堆叠数为 1 的物品，例如多数工具、武器和盔甲。这些物品会继续尝试进入同一 Tom 网络中的普通箱子。
- 抽取永远允许，因此管理员改变规则后，原有物品仍可取出。
- 服务器可通过黑名单、标签和 NBT 大小限制禁止新的物品种类进入。
- 物品种类名额用尽后，不能再加入新的种类；已经存在的种类仍可继续增加数量。

## 常用管理操作

日常管理都可以在访问器右键界面完成：创建、绑定、清除绑定、重命名、删除和升级。也可使用以下命令：

```text
/digitalstorage volume create <名称>
/digitalstorage volume list
/digitalstorage volume rename <存储卷 UUID> <新名称>
/digitalstorage volume delete <存储卷 UUID>

/digitalstorage accessor bind <x> <y> <z> <存储卷 UUID>
/digitalstorage accessor clear <x> <y> <z>
/digitalstorage accessor inspect <x> <y> <z>
```

`/dsc` 可作为短命令。只有内容为空且没有已加载访问器绑定的存储卷可以删除。
`/digitalstorage volume list` 可查询自己的存储卷 UUID。

## 常见问题

### 右键后显示“尚未绑定”

先在界面创建存储卷，再从列表中选择并绑定。也可以使用 `/digitalstorage` 命令。

### Tom 终端里看不到数字存储

确认访问器已经绑定，并把 Tom 库存连接器直接贴在访问器旁，或通过库存线缆连接器正确接入同一个 Tom 网络。然后在访问器界面点击“重新分析”。

### 工具或盔甲放不进去

这是默认设计。数字存储主要保存大宗可堆叠物品；给 Tom 网络同时连接普通箱子后，不可堆叠物品会回落到普通箱子。

### 挖掉访问器后物品是否会消失

不会。物品属于玩家的存储卷，而不是访问器方块。默认只会清除掉落访问器上的绑定信息，重新放置并绑定原存储卷即可。

### 多人能否共用一个存储卷

当前存储卷归创建它的玩家所有，其他玩家不能把访问器绑定到该卷。访问器的方块访问控制也不等同于共享存储卷所有权。
